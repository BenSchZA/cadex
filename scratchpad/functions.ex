defmodule Cadex.Functions do
  # defmacro def_policy(expr, do: block) do
  # end
  # defmacro def_state_update_function(msg, from, state) do
  #   quote do
  #     state = %State{current: current, delta: delta}

  #     unquote(block)

  #     delta_ = %{var => func}

  #     state_ =
  #       state
  #       |> Map.put(:delta, Map.merge(delta, delta_))

  #     {
  #       :reply,
  #       state_,
  #       state_
  #     }
  #   end
  # end
end
